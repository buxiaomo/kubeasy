---
# tasks file for kube-apiserver
- name: Install the binary
  copy: 
    src: '{{ item }}'
    dest: /usr/local/bin/
    mode: 0755
    owner: root
    group: root
  with_items:
    - kube-apiserver

- name: Create log directory
  file: 
    name: '{{ kubernetes.apiserver.log.logDir }}'
    state: directory
    mode: '0755'

- name: Create ssl directory
  file: 
    name: '{{ item }}'
    state: directory
    mode: '0755'
  with_items:
    - '{{ kubernetes.ssl.location }}'
    - '{{ kubernetes.ssl.location }}/etcd'

- name: Distribution of etcd certificate
  copy: 
    src: '/etc/ssl/{{ kubernetes.project }}/etcd/ca.crt'
    dest: '{{ kubernetes.ssl.location }}/etcd'
    owner: root
    group: root

- name: Distribution of certificate
  copy: 
    src: '{{ item }}'
    dest: '{{ kubernetes.ssl.location }}'
    owner: root
    group: root
  with_items:
    - '/etc/ssl/{{ kubernetes.project }}/ca.crt'
    - '/etc/ssl/{{ kubernetes.project }}/apiserver-etcd-client.crt'
    - '/etc/ssl/{{ kubernetes.project }}/apiserver-etcd-client.key'
    - '/etc/ssl/{{ kubernetes.project }}/apiserver-kubelet-client.crt'
    - '/etc/ssl/{{ kubernetes.project }}/apiserver-kubelet-client.key'
    - '/etc/ssl/{{ kubernetes.project }}/sa.pub'
    - '/etc/ssl/{{ kubernetes.project }}/sa.crt'
    - '/etc/ssl/{{ kubernetes.project }}/apiserver.crt'
    - '/etc/ssl/{{ kubernetes.project }}/apiserver.key'
    - '/etc/ssl/{{ kubernetes.project }}/front-proxy-ca.crt'
    - '/etc/ssl/{{ kubernetes.project }}/front-proxy-client.crt'
    - '/etc/ssl/{{ kubernetes.project }}/front-proxy-client.key'
    - '/etc/ssl/{{ kubernetes.project }}/admin.crt'
    - '/etc/ssl/{{ kubernetes.project }}/admin.key'

- name: Stat encryption config file
  stat: 
    path: /etc/kubernetes/pki/encryption-config.yaml
  register: encryption_config_file

- name: Register encryption config
  shell: head -c 32 /dev/urandom | base64
  register: encryption_config
  run_once: true
  when: not encryption_config_file.stat.exists

- name: Install encryption config
  template: 
    src: encryption-config.yaml.j2
    dest: /etc/kubernetes/pki/encryption-config.yaml
  when: not encryption_config_file.stat.exists

- name: Install audit policy
  template: 
    src: audit-policy-minimal.yaml.j2
    dest: /etc/kubernetes/pki/audit-policy-minimal.yaml

- name: Install systemd file
  template: 
    src: kube-apiserver.service.j2
    dest: /etc/systemd/system/kube-apiserver.service

- name: Start the service
  systemd:
    name: kube-apiserver
    state: restarted
    enabled: yes
    daemon_reload: yes

- name: Wait ApiServer
  wait_for:
    port: 6443
    delay: 30
    state: started

- name: Health check
  uri: 
    url: https://{{ ansible_default_ipv4.address }}:6443/healthz
    validate_certs: no
    return_content: yes
  register: this
  until: "'ok' in this.content"
  retries: 30
  delay: 5
  connection: local