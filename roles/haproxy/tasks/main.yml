---
# tasks file for haproxy
- block:
    - name: Install applications
      package: 
        name: '{{ item }}'
        state: present
      with_items:
        - haproxy

    - name: Create applications configure files
      template: 
        src: '{{ item.src }}'
        dest: '{{ item.dest }}'
      with_items:
        - { src: 'haproxy.cfg.j2', dest: '/etc/haproxy/haproxy.cfg'}

    - name: Start the service
      systemd:
        name: haproxy
        enabled: yes
        state: restarted
  when: kubernetes.cloud.type == "local"